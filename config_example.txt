# this is a comment
# common properties are not in blocks
trace_path=path/to/traces/ # comment?
num_switches=1


# condition_<>  
# ...
# condition_end
# blocks are used to define properties of a switch
# switch 1 profile
condition_1
timeout=10

condition_end

# switch 2 profile
condition_2
timeout=100

condition_end